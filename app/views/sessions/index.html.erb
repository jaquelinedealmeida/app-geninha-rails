<h1> GENINHA CARE 👧🏽</h1>
<h2> 🧑🏾‍⚕️ Psychological Care </h2>

<h3>Schedule your sessions 📅 </h3>

Our blog has <%= Participant.public_count %> participants!

<ul>
    <% @sessions.each do |session| %>
        <% unless session.archived? %>
            <li>
                 <%= link_to session.title, session %>
            </li>
        <% end %>
    <% end %>
</ul>

<%= link_to "New Session", new_session_path %>

